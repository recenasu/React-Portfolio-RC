// This is the Contact page, displayed when the Contact option is selected in the Nav bar.
import Contact from '../components/Contact';

export default function ContactPage() {
    return (
    <Contact />
    );
};